{
  "name": "server_init",
  "version": "1.0.0",
  "description": "",
  "main": "webapp.js",
  "scripts": {
    "build:digitalocean": "npm install --production=false && npm run build && npm ci && npm run build:digitalocean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "http-proxy": "^1.18.1",
    "pug": "^3.0.2",
    "socket.io": "^4.6.1",
    "socketio": "^1.0.0"
  }
}
